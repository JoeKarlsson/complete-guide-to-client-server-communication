{
  "name": "http2-demo",
  "version": "1.0.0",
  "description": "HTTP/2 example demonstrating modern HTTP features like multiplexing and binary framing",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "generate-certs": "openssl genrsa -out server.key 2048 && openssl req -new -x509 -key server.key -out server.crt -days 365 -subj \"/C=US/ST=CA/L=San Francisco/O=Test/OU=Test/CN=localhost\"",
    "setup": "npm run generate-certs && npm start"
  },
  "keywords": [
    "http2",
    "server-push",
    "multiplexing",
    "modern-http"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "engines": {
    "node": ">=12.0.0"
  }
}
